/** Titan Frontend V2 — Release version (Canonical Final Reference). */

/** Product version shown in brand chrome — aligned with config/settings.py. */
export const TITAN_PRODUCT_VERSION = "0.43.0";

/** Semantic UI finalization version (frontend composition rebuild). */
export const TITAN_UI_VERSION = "0.51.0";

/** Display label for sidebar brand — matches canonical reference (v0.43.0). */
export const TITAN_UI_VERSION_LABEL = `v${TITAN_PRODUCT_VERSION}`;

/** Internal UI build label (telemetry / diagnostics). */
export const TITAN_UI_BUILD_LABEL = `V${TITAN_UI_VERSION}`;
